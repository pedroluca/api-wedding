; Sem isso, o PHP rejeita uploads acima de 2MB antes mesmo do código da
; API rodar (default do upload_max_filesize), mesmo com o limite de 10MB
; validado em handlers/admin_gifts.php.
upload_max_filesize = 12M
post_max_size = 12M
